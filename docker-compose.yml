services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: kumo
      POSTGRES_PASSWORD: kumo
      POSTGRES_DB: kumo
    ports:
      - "5432:5432"
    volumes:
      - kumo_pgdata:/var/lib/postgresql/data

volumes:
  kumo_pgdata:
